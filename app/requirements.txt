# Trò Cầu Vồng (KOON) — backend
fastapi
uvicorn[standard]
openai            # OpenRouter (OpenAI-compatible) — chấm đáp án
rapidfuzz         # fuzzy match dự phòng khi chưa có LLM
soundfile         # ghi file WAV từ Kokoro TTS

# STT chạy ở browser (Web Speech API: Chrome=Edge) — không cần package Python.
# TTS động: Kokoro Vietnamese — cài riêng:  pip install -e "ref/Kokoro-Vietnamese[onnx]"
